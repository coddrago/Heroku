services:
  worker:
    container_name: heroku_ub
    build:
      context: .
    volumes:
      - worker:/data
    stop_signal: SIGINT
    restart: unless-stopped
    ports:
      - "${EXTERNAL_PORT:-8080}:8080"
    stdin_open: true
    tty: true
    init: true

volumes:
  worker:
